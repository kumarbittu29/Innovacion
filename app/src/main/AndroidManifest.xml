<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.sae_akgec.www.innovacion18">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity
            android:name=".LedActivity"
            android:label="@string/title_activity_led" />
        <activity
            android:name=".BotActivity"
            android:label="@string/title_activity_bot" />
        <activity
            android:name=".Developer"
            android:label="@string/title_activity_developer" />
        <activity
            android:name=".SocietyActivity"
            android:label="@string/Club" />
        <activity
            android:name=".ListDevices"
            android:label="Paired Devices"
            android:theme="@android:style/Theme.Material.Light.Dialog.Alert" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>



    </application>

</manifest>